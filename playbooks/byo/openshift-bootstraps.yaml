---
- include: openshift-cluster/initialize_groups.yml
  tags:
  - always

- include: ../common/openshift-cluster/std_include.yml
  tags:
  - always

- name: Openshift Bootstraps
  hosts: oo_first_master
  become: yes
  roles:
  - role: openshift_bootstraps